fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
httpx>=0.27,<1
psutil>=6,<8
PyYAML>=6,<7
python-multipart>=0.0.12,<1
PyJWT[crypto]>=2.10,<3
Pillow>=11,<13
qrcode[pil]>=8,<9
# ADAS SI page rendering -- inline document display in chat.
# Apache-2.0/BSD, prebuilt Windows wheels, no external binaries.
pypdfium2>=4.30
# Text extraction for source-backed OEM page evidence. PDFium remains the
# runtime fallback, but pypdf handles ordinary embedded text more faithfully.
pypdf>=6,<7
# Local OCR for scan-only/mixed OEM PDFs. RapidOCR runs through ONNX Runtime
# on CPU so reading documents cannot evict or starve X's 30B GPU worker.
rapidocr>=3.9,<4
onnxruntime>=1.28,<2
# Server-side authenticated research browser. Uses the installed Chrome
# channel first; bundled Chromium is only a fallback if separately installed.
playwright>=1.62,<2
